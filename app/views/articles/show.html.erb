<div>
<h1><%= link_to @article.title, @article %></h1>
<p>Published <%= @article.created_at.strftime('%b %d, %Y') %></p>
<p>Filed under: <% @article.categories.each do |category| %>
	<%= link_to category.name, category %>&nbsp;<% end %>
</p>
<div><%= simple_format(@article.content)%></div>

</div>
